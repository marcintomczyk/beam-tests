VS Code hints:
    - terminal: profile should be 'cmd', not Python nor PowerShell
        - only cmd will activate virtualenv automatically
        - make it the default profile if you want to have virtualenv activated automatically after vscode restart 

Other hints:
python -m unittest tests/out_of_order_1.py